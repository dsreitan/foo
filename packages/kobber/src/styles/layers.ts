/**
 * z-index scale. Components never use raw numbers; add a layer here
 * when a new stacking need appears.
 */
export const zIndex = {
  base: 0,
  dropdown: 1000,
  navigation: 1100,
  overlay: 1200,
  toast: 1300,
};
