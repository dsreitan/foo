# Quote Module

Pull quote with optional round image and attribution.

## Built-in behavior

- `<figure>` / `<blockquote>` / `<figcaption>` semantics

## Usage responsibilities

- The image is usually the quoted person: give it a meaningful alt.
- Quote text is content, not a heading — don't use it as the section title.
